name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    name: ${{ matrix.os }} / ${{ matrix.build_type }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        build_type: [Debug, Release]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure CMake
        run: >
          cmake -S . -B build
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DBYTESTREAM_BUILD_TESTS=ON
          -DBYTESTREAM_BUILD_EXAMPLES=ON
          -DBYTESTREAM_BUILD_BENCHMARKS=OFF

      - name: Build
        run: cmake --build build --config ${{ matrix.build_type }}

      # single-config (Linux/macOS): no --config
      - name: Test (Unix)
        if: runner.os != 'Windows'
        working-directory: build
        run: ctest --output-on-failure

      # multi-config (Windows): needs --config
      - name: Test (Windows)
        if: runner.os == 'Windows'
        working-directory: build
        run: ctest --output-on-failure --config ${{ matrix.build_type }}
